//Syntax => array.join(separator) 
//join() returns a new string by concatenating all of elements in an array, separted by commas or a specified saparator string.

let bikes = ["Yamaha", "Bajaj", "Honda", "TVS"];
console.log(bikes.join());
console.log(bikes.join("")); //without spacing
console.log(bikes.join("-"));