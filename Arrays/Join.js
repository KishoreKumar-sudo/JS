//Syntax => array.join(separator) 
//the join() returns a new string by concatenating all of elements in an array, separted by commas or a specified saparator string.

let bikes = ["Yamaha", "Bajaj", "Honda", "TVS"]
console.log(bikes.join())